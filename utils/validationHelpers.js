/**
 * @deprecated This file is no longer imported anywhere in the codebase.
 *
 * Validation has been consolidated into the Joi schema in screeningController.js.
 * Skill/education/experience validation utilities that were here have been
 * superseded by the inline logic in matchingEngine.js.
 *
 * This file is kept to avoid a hard crash if any old code still requires it,
 * but it should be deleted in the next cleanup pass.
 */

module.exports = {};
